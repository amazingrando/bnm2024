<button type="button" id="{{ id }}" class="text-gold-1000 hover:bg-gold-500 bg-gold-600 text-lg py-2 px-4 rounded-sm flex flex-row gap-2 items-center">
	{{ label }}

	{% if iconAfter %}
		<i class="{{iconAfter}}"></i>
	{% endif %}
</button>
