<h{{level|default(2)}} {% if modifier %} class="{{ modifier }} bg-red-500" {% endif %}>
	{% if url %}
		<a href="{{ url }}">{{ text }}</a>
	{% else %}
		<span>{{ text }}</span>
	{% endif %}
</h{{level|default(2)}}>
